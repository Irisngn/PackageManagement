This is a simple web application that records package management data and supports full CRUD operations.
To run the project, install all dependencies and use npm run dev to start the server.
The application also integrates AI from the OpenAI API for translation and uses an external API to answer questions.
